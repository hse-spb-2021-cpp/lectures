// variables + fields
// default vs value
// copy vs direct
// CTAD
// + temporaries

// the most vexing parse

// + object by const ref can actually change

// + std::move (initialize person name); no need to return std::move()
// return by reference is dangerous: plain, chains in range-based-for.
